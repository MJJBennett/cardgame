/*
use std::collections::VecDeque;
use crate::cards::*;
use crate::errors::*;

struct EventID(u32);

impl EventID {
     
}

enum Event {
    Play(Card, Target, EventID),
    Block(Card, Target, EventID),
    Challenge(Target, EventID),
}

trait EventHandler {
    fn handle(&mut self, event: &Event) -> GameResult<()>; 
}
*/
